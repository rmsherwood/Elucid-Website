{#
/**
 * @file
 * The header region template.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region div.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 *
 * @ingroup themeable
 */
#}
<div id="region_header" {{ attributes }}>
  <div id="region_header_left">
    region_header_left
    {{ render_block('template_logo_block') }}
  </div>
  <div id="region_header_right">
    region_header_right
    {{ content }}
  </div>
</div>
